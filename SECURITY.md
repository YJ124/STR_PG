# Security policy

Do not report sensitive vulnerabilities in a public issue. After the repository
is created, use GitHub's private security-advisory feature. Until a private
reporting channel is configured, this release candidate should not be published
as a security-supported production service.

Never attach patient data, access tokens, SSH keys, or private sequencing files
to a report.
